import { getComponentTitle, metadataMap } from "@/lib/og"

export const metadata = {
  title: getComponentTitle("command"),
  ...metadataMap["command"],
}

<PreviewComponentWithCode componentName="command" />

<Separator />

# Installation

<CommandWrapper componentName={"command"} />
